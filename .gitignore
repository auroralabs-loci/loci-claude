# Runtime state and logs
loci/state/
state/
.loci-build/
.setup-complete
.setup-lock/
.welcome-shown
*.log
*.pid
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/

# Python build artifacts (loci_cli wheel)
dist/
build/
*.egg-info/

# LOCI's per-project build root: compiled objects, turn state, manifests.
# Named explicitly rather than left to the bare `build/` rule above, which
# catches it only incidentally — tightening that rule to `/build/` would make
# turn state committable.
.loci/build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

.claude

# Eval output
eval-results/

.local/
