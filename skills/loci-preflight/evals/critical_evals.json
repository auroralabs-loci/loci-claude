{
  "skill_name": "loci-preflight",
  "description": "Critical smoke tests for the loci-preflight plan-mode GATE — the single behavior that matters most: does the skill invoke when (and only when) the user is in /plan mode? Per SKILL.md, preflight is 'MANDATORY in /plan mode' and must 'NOT invoke ... outside plan mode'. The suite is five tests in two controlled groups: (1) three /plan prompts that MUST invoke, (2) two no-/plan prompts whose wording SOUNDS like planning ('how should I modify', 'walk me through the plan') but, lacking the /plan prefix, MUST stay silent. Tests are paired by task so the ONLY difference between an invoke and a no-invoke case is the /plan signal: pf-critical-1 <-> pf-critical-5 (bounds guard on Connection_getConnIndex) and pf-critical-2 <-> pf-critical-4 (TX byte counter in HCI_SendDataPkt). All functions are compiled into $LOCI_TEST_BLE_ROOT/CC2340R5/basic_ble.out, so a real /plan run produces a full report (not a STOPPED block).",
  "note": "Set $LOCI_TEST_BLE_ROOT to your local BLE project root before running. NOTE: run_evals.sh keeps a leading '/plan' verbatim (it strips other slash commands) — that prefix is the plan-mode signal these tests gate on. Bash grading detects invocation via the '## Preflight:' header plus an 'Execution fit:' verdict line, matching the current SKILL.md output format.",
  "evals": [
    {
      "id": "pf-critical-1",
      "should_trigger": true,
      "category": "invoke-plan-mode-implement-bounds-guard",
      "grading_mode": "bash",
      "prompt": "/plan Implement a bounds guard at the top of Connection_getConnIndex in $LOCI_TEST_BLE_ROOT/examples/rtos/LP_EM_CC2340R53/ble5stack/basic_ble_profiles/app/app_connection.c that returns -1 immediately if the connection handle is greater than or equal to MAX_NUM_BLE_CONNS, so callers never index past the connection table. The function is compiled into $LOCI_TEST_BLE_ROOT/CC2340R5/basic_ble.out.",
      "expected_output": "User is in /plan mode and describes new logic, so preflight is MANDATORY. A preflight report appears in the response text (before any plan body): a '## Preflight: Connection_getConnIndex' header, a conclusion table, and a bolded 'Execution fit: GOOD|ADJUST PLAN|STOP' verdict.",
      "files": [],
      "assertions": [
        {
          "text": "Response includes a '## Preflight:' section header",
          "description": "/plan mode + 'Implement a bounds guard' (new logic) must invoke the skill"
        },
        {
          "text": "Response includes an 'Execution fit:' line with value GOOD, ADJUST PLAN, or STOP",
          "description": "A proper invocation produces the verdict line per the SKILL.md output format"
        }
      ]
    },
    {
      "id": "pf-critical-2",
      "should_trigger": true,
      "category": "invoke-plan-mode-how-should-i-modify-counter",
      "grading_mode": "bash",
      "prompt": "/plan How should I modify HCI_SendDataPkt in $LOCI_TEST_BLE_ROOT/simplelink-lowpower-f3-sdk/source/ti/ble5stack_flash/hci/cc26xx/hci.c to add a static uint32_t per-connection TX byte counter that accumulates the dataLen parameter on each successful send and wraps silently? The function is compiled into $LOCI_TEST_BLE_ROOT/CC2340R5/basic_ble.out.",
      "expected_output": "User is in /plan mode and the wording 'How should I modify' is an explicit SKILL.md trigger, so preflight is MANDATORY. A '## Preflight: HCI_SendDataPkt' report with an 'Execution fit:' verdict appears in the response before the modification plan. (This is the positive twin of pf-critical-4, which is the same task WITHOUT /plan.)",
      "files": [],
      "assertions": [
        {
          "text": "Response includes a '## Preflight:' section header",
          "description": "/plan mode + 'How should I modify' must invoke the skill"
        },
        {
          "text": "Response includes an 'Execution fit:' line with value GOOD, ADJUST PLAN, or STOP",
          "description": "A proper invocation produces the verdict line per the SKILL.md output format"
        }
      ]
    },
    {
      "id": "pf-critical-3",
      "should_trigger": true,
      "category": "invoke-plan-mode-add-new-callee-timing-budget",
      "grading_mode": "bash",
      "prompt": "/plan Add a bond check inside HCI_SendDataPkt in $LOCI_TEST_BLE_ROOT/simplelink-lowpower-f3-sdk/source/ti/ble5stack_flash/hci/cc26xx/hci.c that calls GAPBondMgr_FindAddr to reject packets from unbonded peers before the packet is sent. The HCI data path must complete within 10us at 48 MHz. Both functions are compiled into $LOCI_TEST_BLE_ROOT/CC2340R5/basic_ble.out.",
      "expected_output": "User is in /plan mode and describes adding a new callee on a timing-constrained hot path, so preflight is MANDATORY. A '## Preflight: HCI_SendDataPkt' report appears with an 'Execution fit:' verdict — likely ADJUST PLAN or STOP given the NV-reading GAPBondMgr_FindAddr against a sub-10us budget, but any GOOD/ADJUST PLAN/STOP verdict satisfies the invocation gate.",
      "files": [],
      "assertions": [
        {
          "text": "Response includes a '## Preflight:' section header",
          "description": "/plan mode + 'Add a bond check' that introduces a new callee must invoke the skill"
        },
        {
          "text": "Response includes an 'Execution fit:' line with value GOOD, ADJUST PLAN, or STOP",
          "description": "A proper invocation produces the verdict line per the SKILL.md output format"
        }
      ]
    },
    {
      "id": "pf-critical-4",
      "should_trigger": false,
      "category": "no-invoke-no-plan-mode-how-should-i-modify-counter",
      "grading_mode": "bash",
      "prompt": "How should I modify HCI_SendDataPkt in $LOCI_TEST_BLE_ROOT/simplelink-lowpower-f3-sdk/source/ti/ble5stack_flash/hci/cc26xx/hci.c to add a static uint32_t per-connection TX byte counter that accumulates the dataLen parameter on each successful send and wraps silently? The function is compiled into $LOCI_TEST_BLE_ROOT/CC2340R5/basic_ble.out.",
      "expected_output": "Identical task to pf-critical-2 but with NO /plan prefix — the user is NOT in plan mode. The phrase 'How should I modify' is a SKILL.md trigger word, but SKILL.md gates triggers on plan mode ('Do NOT invoke ... outside plan mode'). The skill must stay silent: a direct answer with NO '## Preflight:' header and NO 'Execution fit:' verdict.",
      "files": [],
      "assertions": [
        {
          "text": "Response does NOT contain a '## Preflight:' header",
          "description": "No /plan prefix — plan-sounding trigger wording must NOT invoke the skill outside plan mode"
        },
        {
          "text": "Response does NOT contain an 'Execution fit:' verdict line",
          "description": "No preflight report should be emitted when the user is not in plan mode"
        }
      ]
    },
    {
      "id": "pf-critical-5",
      "should_trigger": false,
      "category": "no-invoke-no-plan-mode-walk-me-through-the-plan",
      "grading_mode": "bash",
      "prompt": "How should I approach adding a bounds guard at the top of Connection_getConnIndex in $LOCI_TEST_BLE_ROOT/examples/rtos/LP_EM_CC2340R53/ble5stack/basic_ble_profiles/app/app_connection.c so it returns -1 when the connection handle is greater than or equal to MAX_NUM_BLE_CONNS?",
      "expected_output": "Identical task to pf-critical-1 but with NO /plan prefix. The wording ('How should I approach', 'Walk me through the plan before we implement') strongly sounds like planning and could fool the model into thinking it is in plan mode — but no /plan was entered, so plan mode is NOT active. Per SKILL.md the skill must stay silent: NO '## Preflight:' header and NO 'Execution fit:' verdict. The model may discuss the approach in prose, just without invoking preflight.",
      "files": [],
      "assertions": [
        {
          "text": "Response does NOT contain a '## Preflight:' header",
          "description": "Plan-sounding wording without a /plan prefix must NOT invoke preflight — this is the core false-positive guard"
        },
        {
          "text": "Response does NOT contain an 'Execution fit:' verdict line",
          "description": "No preflight report should be emitted when the user is not actually in plan mode"
        }
      ]
    }
  ]
}
